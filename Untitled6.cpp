#include <stdio.h>
#include <math.h> // Necessário para usar pow()

int main() {
    double resultado = pow(2.0, 3.0); // 2 elevado a 3
    printf("Resultado: %.2lf\n", resultado);
    return 0;
}
